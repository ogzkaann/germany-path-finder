# Germany Path Finder is local-first and does not require a backend.
# API settings are entered by the user in the BYOK settings modal and stored in localStorage.
# Do not commit API keys.

VITE_APP_NAME="Germany Path Finder"
